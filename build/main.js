// Generated by CoffeeScript 1.3.3
(function() {
  var $, c, nameIdentifier;

  $ = window.$;

  c = typeof console !== "undefined" && console !== null ? console : {
    log: $.noop
  };

  nameIdentifier = function(text) {
    var match, matches, modify, re, textArray, _i, _len, _ref;
    re = /([A-ZÅÖÄ]\w+(\s([A-ZÅÖÄ])*(\.)*(\s)*[A-ZÅÖÄ]\w+)*)/g;
    textArray = text.split("");
    modify = function(match) {
      var end, newWord, start, _ref;
      start = match.index;
      end = start + match[0].length - 1;
      newWord = match[0].replace(/\s/g, '_');
      return ([].splice.apply(textArray, [start, end - start + 1].concat(_ref = ["<" + newWord + ">"])), _ref);
    };
    matches = (function() {
      var _results;
      _results = [];
      while (match = re.exec(text)) {
        _results.push(match);
      }
      return _results;
    })();
    _ref = matches.reverse();
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      match = _ref[_i];
      modify(match);
    }
    return textArray.join("");
  };

  $(function() {
    c.log("starting up");
    $("#content form").submit(function() {
      c.log("submit");
      return false;
    });
    return c.log(nameIdentifier("this is a test for Finding Some words That Are Names."));
  });

}).call(this);
